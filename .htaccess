# <IfModule mod_rewrite.c>
#     # 1. Mengaktifkan Rewrite Engine
#     RewriteEngine On

#     # 2. Arahkan semua request ke folder public
#     RewriteRule ^(.*)$ public/$1 [L]
# </IfModule>

# # 3. PENTING: Mencegah orang mendownload file .env Anda
# <FilesMatch "^\.env|composer\.json|composer\.lock|package\.json|phpunit\.xml">
#     Order allow,deny
#     Deny from all
# </FilesMatch>

# # 4. Mencegah directory listing (agar orang tidak bisa melihat isi folder)
# Options -Indexes